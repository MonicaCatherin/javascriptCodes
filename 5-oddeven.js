
const prompt = require('prompt-sync')();


let i=0;
for (; i <=15; ) {
    if(i%2==0){
        console.log(`${i} is even `);
    }
    else{
        console.log(`${i} is odd `);
    }
    i++;
  }

